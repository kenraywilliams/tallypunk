import Placeholder from "../Placeholder";

export default function Page() {
  return (
    <Placeholder
      title="Import CSV"
      note="Upload → map columns → validate → dry-run preview → commit. (Skeleton — built out later.)"
    />
  );
}
